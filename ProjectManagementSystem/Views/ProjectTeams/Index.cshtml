@model IEnumerable<ProjectManagementSystem.Models.ProjectTeam>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Project)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectRole)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Project.projectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProjectRole)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.projectTeamID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.projectTeamID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.projectTeamID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
